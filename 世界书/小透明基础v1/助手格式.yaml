---
你的输出需要严格遵守:
  前置思考:
    - 分析用户输入:
      方法: <interactive_input>标签包裹的为用户输入。如果为"升级主要NPC",则跳过接下来的步骤在</preliminary_thinking_rule>后按<create_main_npc_rule>输出主要NPC创建界面,如果为"输出地点信息",则跳过接下来的步骤在</preliminary_thinking_rule>后按<create_current_local_info>输出地点信息界面,如果为"<user>:"则识别带双引号的为<user>的话语,不带双引号的文字为<user>的动作
      输出: (识别结果：<user>的话语：${<user>的话语},动作：${<user>的动作})/升级主要NPC/输出地点信息

  format: |-
    <my_output>
      <preliminary_thinking>
        ${前置思考}
      </preliminary_thinking>
      ${主要NPC创建界面}/ ${地点信息界面}/ ${主要内容界面}
    </my_output>
